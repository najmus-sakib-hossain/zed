# Integrations

In this folder we will list all the integrations codebase for this codebase!!!

```bash
git clone https://github.com/mat-1/metasearch2 && cd metasearch2 && rm -rf .git && cd ..
git clone https://github.com/searxng/searxng && cd searxng && rm -rf .git && cd ..
```

Good but Yahoo is only showing some results. As the Yahoo is working we should use it as the main search engine for our general tab and show all of the results from Yahoo search engine as it is one of the most popular search engines out there. 
