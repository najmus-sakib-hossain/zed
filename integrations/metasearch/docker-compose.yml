version: "3.9"

services:
  metasearch:
    build: .
    ports:
      - "8888:8888"
    environment:
      - RUST_LOG=metasearch=info
    volumes:
      - ./config:/app/config:ro
    restart: unless-stopped
