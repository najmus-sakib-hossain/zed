
# dx-www-fallback

HTML fallback mode for dx-www.

## Overview

This crate provides HTML fallback rendering using Maud templates for browsers that don't support WASM or when JavaScript is disabled.

## Installation

```toml
[dependencies]
dx-www-fallback = { path = "../fallback" }
```

## License

MIT OR Apache-2.0
