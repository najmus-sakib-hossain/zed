[package]
name = "dx-www-demo"
version = "1.0.0"
edition = "2024"

[[bin]]
name = "demo-server"
path = "server.rs"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.5", features = ["fs"] }
dx-www-binary = { path = "../binary" }
ed25519-dalek = "2"
