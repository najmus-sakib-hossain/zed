[package]
name = "dx-www-error"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Binary error boundaries"
keywords = ["error", "boundaries", "binary", "handling", "recovery"]
categories = ["rust-patterns"]

[lib]
path = "src/lib.rs"

[dependencies]
console_error_panic_hook = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
tracing = "0.1"

[dev-dependencies]
proptest = "1.4"
