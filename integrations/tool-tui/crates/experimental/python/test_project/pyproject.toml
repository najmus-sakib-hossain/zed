[project]
name = "my-project"
version = "0.1.0"
description = "A Python project: my-project"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
