[package]
name = "dx-bundle-delta"
description = "Delta/incremental bundling for DX Bundler v2"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
dx-bundle-core = { path = "../dx-bundle-core" }
dashmap.workspace = true
