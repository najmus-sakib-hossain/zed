[package]
name = "dx-compat-plugin"
version = "0.0.1"
edition = "2021"
description = "Plugin system for DX"
license = "MIT OR Apache-2.0"

[dependencies]
regex = "1.11"
thiserror = "2.0"
parking_lot = "0.12"

[dev-dependencies]
proptest = "1.4"
