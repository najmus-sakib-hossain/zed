[package]
name = "dx-compat-web"
version = "0.0.1"
edition = "2021"
description = "Web Standard APIs compatibility layer for DX"
license = "MIT OR Apache-2.0"

[dependencies]
# HTTP client - using native-tls to avoid aws-lc-sys build issues on Windows
# native-tls uses SChannel on Windows, SecureTransport on macOS, OpenSSL on Linux
reqwest = { version = "0.12", features = ["json", "stream", "native-tls"], default-features = false }
http = "1.1"

# Async runtime
tokio = { version = "1.41", features = ["sync", "time"] }
futures = "0.3"

# Binary data
bytes = "1.9"

# WebSocket
tokio-tungstenite = "0.24"

# Compression
flate2 = "1.0"

# URL parsing
url = "2.5"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error handling
thiserror = "2.0"

[dev-dependencies]
proptest = "1.4"
tokio = { version = "1.41", features = ["full", "test-util"] }
