# DX Onboard Environment Template
#
# Copy this file to `.env` (or `root.env`) at the repo root and fill values.
# Loaded by: crates/cli-onboard/src/main.rs

# ============================================================
# GOOGLE ANTIGRAVITY (recommended provider in onboarding)
# ============================================================
# You can authenticate in either style:
# 1) ACCESS TOKEN directly (short-lived)
# 2) REFRESH TOKEN (preferred), optionally with project id via `refresh|project_id`
#
# Where to get these values:
# - Google OAuth docs (official):
#   https://developers.google.com/identity/protocols/oauth2
#   https://developers.google.com/identity/protocols/oauth2/native-app
# - Practical flow used by the Antigravity plugin:
#   https://github.com/NoeFabris/opencode-antigravity-auth
#
# Easiest practical method today:
# - Run `opencode auth login` with the Antigravity plugin installed.
# - After login, copy `refreshToken` (and optionally `projectId`) from:
#   Windows: %USERPROFILE%/.config/opencode/antigravity-accounts.json
#   macOS/Linux: ~/.config/opencode/antigravity-accounts.json

# Option A: Access token (ya29...)
GOOGLE_ANTIGRAVITY_ACCESS_TOKEN=

# Option B (preferred): refresh token (1//...) optionally with project id:
# Example: 1//abc123...|my-gcp-project-id
GOOGLE_ANTIGRAVITY_REFRESH_TOKEN=

# Back-compat alias used by onboarding logic.
# Can be either:
# - access token
# - refresh token
# - refresh token with project id: refresh_token|project_id
GOOGLE_ANTIGRAVITY_API_KEY=

# Optional: force project id (if auto-discovery/loadCodeAssist fails).
# Create/select project at: https://console.cloud.google.com/
# If needed, enable Gemini for Google Cloud API (cloudaicompanion.googleapis.com).
GOOGLE_ANTIGRAVITY_PROJECT_ID=

# Optional: custom endpoint override (normally leave empty)
# Default fallback is handled internally.
GOOGLE_ANTIGRAVITY_BASE_URL=

# ============================================================
# GITHUB COPILOT
# ============================================================
# Preferred: sign in via VS Code GitHub Copilot extension, then onboarding auto-detects token.
# Copilot setup docs:
# https://docs.github.com/en/copilot
# VS Code extension:
# https://marketplace.visualstudio.com/items?itemName=GitHub.copilot
#
# Auto-detect file locations used by this repo on Windows:
# - %LOCALAPPDATA%/github-copilot/hosts.json
# - %LOCALAPPDATA%/github-copilot/apps.json

# Optional explicit token (if you want to force it)
GITHUB_COPILOT_TOKEN=

# Optional GitHub token for exchanging into a Copilot service token.
# The official Copilot CLI supports these env vars (precedence shown here):
# - COPILOT_GITHUB_TOKEN
# - GH_TOKEN
# - GITHUB_TOKEN
#
# If using a PAT, create a fine-grained PAT and grant the "Copilot Requests" permission:
# https://github.com/settings/personal-access-tokens/new
COPILOT_GITHUB_TOKEN=
GH_TOKEN=

# Optional fallback (PAT) - also used by other tooling.
GITHUB_TOKEN=

# Optional tier hints used by logic:
# GITHUB_COPILOT_PAID=1
# GITHUB_COPILOT_ENTERPRISE=1

# ============================================================
# GOOGLE GEMINI (standard API path)
# ============================================================
# Get key from Google AI Studio:
# https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=

# Alias accepted by onboarding (auto-mapped to/from GOOGLE_API_KEY)
GEMINI_API_KEY=

# ============================================================
# GROQ
# ============================================================
# Get key from Groq Console:
# https://console.groq.com/keys
GROQ_API_KEY=
