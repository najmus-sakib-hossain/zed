gpui - Rust

[Docs.rs](/)

* [gpui-0.2.2](# "Zed's GPU-accelerated UI framework")

  + gpui 0.2.2
  + [Docs.rs crate page](/crate/gpui/0.2.2 "See gpui in docs.rs")
  + [Apache-2.0](https://spdx.org/licenses/Apache-2.0)

  + Links
  + [Homepage](https://gpui.rs)
  + [Repository](https://github.com/zed-industries/zed)
  + [crates.io](https://crates.io/crates/gpui "See gpui in crates.io")
  + [Source](/crate/gpui/0.2.2/source/ "Browse source of gpui-0.2.2")

  + Owners
  + [maxbrunsfeld](https://crates.io/users/maxbrunsfeld)
  + [mikayla-maki](https://crates.io/users/mikayla-maki)
  + [github:zed-industries:crates-io](https://crates.io/teams/github:zed-industries:crates-io)

  + Dependencies
  + - [anyhow ^1.0.86
      *normal*](/anyhow/^1.0.86/)
    - [async-task ^4.7
      *normal*](/async-task/^4.7/)
    - [backtrace ^0.3
      *normal*
      *optional*](/backtrace/^0.3/)
    - [blade-graphics ^0.7.0
      *normal*
      *optional*](/blade-graphics/^0.7.0/)
    - [blade-macros ^0.3.0
      *normal*
      *optional*](/blade-macros/^0.3.0/)
    - [blade-util ^0.3.0
      *normal*
      *optional*](/blade-util/^0.3.0/)
    - [bytemuck ^1
      *normal*
      *optional*](/bytemuck/^1/)
    - [gpui\_collections ^0.2.2
      *normal*](/gpui_collections/^0.2.2/)
    - [ctor ^0.4.0
      *normal*](/ctor/^0.4.0/)
    - [derive\_more ^0.99.17
      *normal*](/derive_more/^0.99.17/)
    - [etagere ^0.2
      *normal*](/etagere/^0.2/)
    - [futures ^0.3
      *normal*](/futures/^0.3/)
    - [gpui-macros ^0.2.2
      *normal*](/gpui-macros/^0.2.2/)
    - [gpui\_http\_client ^0.2.2
      *normal*](/gpui_http_client/^0.2.2/)
    - [image ^0.25.1
      *normal*](/image/^0.25.1/)
    - [inventory ^0.3.19
      *normal*](/inventory/^0.3.19/)
    - [itertools ^0.14.0
      *normal*](/itertools/^0.14.0/)
    - [libc ^0.2
      *normal*](/libc/^0.2/)
    - [log ^0.4.16
      *normal*](/log/^0.4.16/)
    - [lyon ^1.0
      *normal*](/lyon/^1.0/)
    - [num\_cpus ^1.13
      *normal*](/num_cpus/^1.13/)
    - [parking ^2.0.0
      *normal*](/parking/^2.0.0/)
    - [parking\_lot ^0.12.1
      *normal*](/parking_lot/^0.12.1/)
    - [pin-project ^1.1.10
      *normal*](/pin-project/^1.1.10/)
    - [postage ^0.5
      *normal*](/postage/^0.5/)
    - [profiling ^1
      *normal*](/profiling/^1/)
    - [rand ^0.9
      *normal*
      *optional*](/rand/^0.9/)
    - [raw-window-handle ^0.6
      *normal*](/raw-window-handle/^0.6/)
    - [gpui\_refineable ^0.2.2
      *normal*](/gpui_refineable/^0.2.2/)
    - [resvg ^0.45.0
      *normal*](/resvg/^0.45.0/)
    - [schemars ^1.0
      *normal*](/schemars/^1.0/)
    - [seahash ^4.1
      *normal*](/seahash/^4.1/)
    - [gpui\_semantic\_version ^0.2.2
      *normal*](/gpui_semantic_version/^0.2.2/)
    - [serde ^1.0.221
      *normal*](/serde/^1.0.221/)
    - [serde\_json ^1.0.144
      *normal*](/serde_json/^1.0.144/)
    - [slotmap ^1.0.6
      *normal*](/slotmap/^1.0.6/)
    - [smallvec ^1.6
      *normal*](/smallvec/^1.6/)
    - [smol ^2.0
      *normal*](/smol/^2.0/)
    - [stacksafe ^0.1
      *normal*](/stacksafe/^0.1/)
    - [strum ^0.27.2
      *normal*](/strum/^0.27.2/)
    - [gpui\_sum\_tree ^0.2.2
      *normal*](/gpui_sum_tree/^0.2.2/)
    - [taffy =0.9.0
      *normal*](/taffy/=0.9.0/)
    - [thiserror ^2.0.12
      *normal*](/thiserror/^2.0.12/)
    - [usvg ^0.45.0
      *normal*](/usvg/^0.45.0/)
    - [gpui\_util ^0.2.2
      *normal*](/gpui_util/^0.2.2/)
    - [gpui\_util\_macros ^0.2.2
      *normal*](/gpui_util_macros/^0.2.2/)
    - [uuid ^1.1.2
      *normal*](/uuid/^1.1.2/)
    - [waker-fn ^1.2.0
      *normal*](/waker-fn/^1.2.0/)
    - [backtrace ^0.3
      *dev*](/backtrace/^0.3/)
    - [gpui\_collections ^0.2.2
      *dev*](/gpui_collections/^0.2.2/)
    - [env\_logger ^0.11
      *dev*](/env_logger/^0.11/)
    - [gpui\_http\_client ^0.2.2
      *dev*](/gpui_http_client/^0.2.2/)
    - [lyon ^1.0
      *dev*](/lyon/^1.0/)
    - [pretty\_assertions ^1.3.0
      *dev*](/pretty_assertions/^1.3.0/)
    - [rand ^0.9
      *dev*](/rand/^0.9/)
    - [unicode-segmentation ^1.10
      *dev*](/unicode-segmentation/^1.10/)
    - [gpui\_util ^0.2.2
      *dev*](/gpui_util/^0.2.2/)
    - [as-raw-xcb-connection ^1
      *normal*
      *optional*](/as-raw-xcb-connection/^1/)
    - [ashpd ^0.11
      *normal*
      *optional*](/ashpd/^0.11/)
    - [blade-graphics ^0.7.0
      *normal*
      *optional*](/blade-graphics/^0.7.0/)
    - [blade-macros ^0.3.0
      *normal*
      *optional*](/blade-macros/^0.3.0/)
    - [blade-util ^0.3.0
      *normal*
      *optional*](/blade-util/^0.3.0/)
    - [bytemuck ^1
      *normal*
      *optional*](/bytemuck/^1/)
    - [calloop ^0.13.0
      *normal*](/calloop/^0.13.0/)
    - [calloop-wayland-source ^0.3.0
      *normal*
      *optional*](/calloop-wayland-source/^0.3.0/)
    - [cosmic-text ^0.14.0
      *normal*
      *optional*](/cosmic-text/^0.14.0/)
    - [filedescriptor ^0.8.2
      *normal*
      *optional*](/filedescriptor/^0.8.2/)
    - [flume ^0.11
      *normal*](/flume/^0.11/)
    - [zed-font-kit ^0.14.1-zed
      *normal*
      *optional*](/zed-font-kit/^0.14.1-zed/)
    - [oo7 ^0.5.0
      *normal*](/oo7/^0.5.0/)
    - [open ^5.2.0
      *normal*
      *optional*](/open/^5.2.0/)
    - [wayland-backend ^0.3.3
      *normal*
      *optional*](/wayland-backend/^0.3.3/)
    - [wayland-client ^0.31.2
      *normal*
      *optional*](/wayland-client/^0.31.2/)
    - [wayland-cursor ^0.31.1
      *normal*
      *optional*](/wayland-cursor/^0.31.1/)
    - [wayland-protocols ^0.31.2
      *normal*
      *optional*](/wayland-protocols/^0.31.2/)
    - [wayland-protocols-plasma ^0.2.0
      *normal*
      *optional*](/wayland-protocols-plasma/^0.2.0/)
    - [x11-clipboard ^0.9.3
      *normal*
      *optional*](/x11-clipboard/^0.9.3/)
    - [x11rb ^0.13.1
      *normal*
      *optional*](/x11rb/^0.13.1/)
    - [zed-xim ^0.4.0-zed
      *normal*
      *optional*](/zed-xim/^0.4.0-zed/)
    - [xkbcommon ^0.8.0
      *normal*
      *optional*](/xkbcommon/^0.8.0/)
    - [naga ^25.0
      *build*](/naga/^25.0/)
    - [pathfinder\_geometry ^0.5
      *normal*](/pathfinder_geometry/^0.5/)
    - [zed-scap ^0.0.8-zed
      *normal*
      *optional*](/zed-scap/^0.0.8-zed/)
    - [block ^0.1
      *normal*](/block/^0.1/)
    - [cocoa =0.26.0
      *normal*](/cocoa/=0.26.0/)
    - [cocoa-foundation =0.2.0
      *normal*](/cocoa-foundation/=0.2.0/)
    - [core-foundation =0.10.0
      *normal*](/core-foundation/=0.10.0/)
    - [core-foundation-sys ^0.8.6
      *normal*](/core-foundation-sys/^0.8.6/)
    - [core-graphics ^0.24
      *normal*](/core-graphics/^0.24/)
    - [core-text ^21
      *normal*](/core-text/^21/)
    - [core-video ^0.4.3
      *normal*](/core-video/^0.4.3/)
    - [zed-font-kit ^0.14.1-zed
      *normal*
      *optional*](/zed-font-kit/^0.14.1-zed/)
    - [foreign-types ^0.5
      *normal*](/foreign-types/^0.5/)
    - [log ^0.4.16
      *normal*](/log/^0.4.16/)
    - [gpui\_media ^0.2.2
      *normal*](/gpui_media/^0.2.2/)
    - [metal ^0.29
      *normal*](/metal/^0.29/)
    - [objc ^0.2
      *normal*](/objc/^0.2/)
    - [objc2 ^0.6
      *normal*
      *optional*](/objc2/^0.6/)
    - [objc2-metal ^0.3
      *normal*
      *optional*](/objc2-metal/^0.3/)
    - [bindgen ^0.71
      *build*](/bindgen/^0.71/)
    - [cbindgen ^0.28.0
      *build*](/cbindgen/^0.28.0/)
    - [naga ^25.0
      *build*](/naga/^25.0/)
    - [flume ^0.11
      *normal*](/flume/^0.11/)
    - [rand ^0.9
      *normal*](/rand/^0.9/)
    - [windows ^0.61
      *normal*](/windows/^0.61/)
    - [windows-core ^0.61
      *normal*](/windows-core/^0.61/)
    - [windows-numerics ^0.2
      *normal*](/windows-numerics/^0.2/)
    - [windows-registry ^0.5
      *normal*](/windows-registry/^0.5/)
    - [embed-resource ^3.0
      *build*](/embed-resource/^3.0/)

  + Versions

  + [**100%**
    of the crate is documented](/crate/gpui/0.2.2)
* [Platform](#)
  + [x86\_64-unknown-linux-gnu](/crate/gpui/0.2.2/target-redirect/gpui/)
* [Feature flags](/crate/gpui/0.2.2/features "Browse available feature flags of gpui-0.2.2")

* [docs.rs](#)
  + [About docs.rs](/about)
  + [Badges](/about/badges)
  + [Builds](/about/builds)
  + [Metadata](/about/metadata)
  + [Shorthand URLs](/about/redirections)
  + [Download](/about/download)
  + [Rustdoc JSON](/about/rustdoc-json)
  + [Build queue](/releases/queue)
  + [Privacy policy](https://foundation.rust-lang.org/policies/privacy-policy/#docs.rs)

* [Rust](#)
  + [Rust website](https://www.rust-lang.org/)
  + [The Book](https://doc.rust-lang.org/book/)
  + [Standard Library API Reference](https://doc.rust-lang.org/std/)
  + [Rust by Example](https://doc.rust-lang.org/rust-by-example/)
  + [The Cargo Guide](https://doc.rust-lang.org/cargo/guide/)
  + [Clippy Documentation](https://doc.rust-lang.org/nightly/clippy)

[Crate gpui](#)
---------------

[gpui](../gpui/index.html)0.2.2
-------------------------------

* [All Items](all.html)

### [Sections](#)

* [Welcome to GPUI!](#welcome-to-gpui "Welcome to GPUI!")
  + [Getting Started](#getting-started "Getting Started")
  + [The Big Picture](#the-big-picture "The Big Picture")
  + [Other Resources](#other-resources "Other Resources")

### [Crate Items](#reexports)

* [Re-exports](#reexports "Re-exports")
* [Modules](#modules "Modules")
* [Macros](#macros "Macros")
* [Structs](#structs "Structs")
* [Enums](#enums "Enums")
* [Constants](#constants "Constants")
* [Traits](#traits "Traits")
* [Functions](#functions "Functions")
* [Type Aliases](#types "Type Aliases")
* [Attribute Macros](#attributes "Attribute Macros")
* [Derive Macros](#derives "Derive Macros")

Crate gpui Copy item path
=========================

[Source](../src/gpui/gpui.rs.html#1-311)

Expand description

[§](#welcome-to-gpui)Welcome to GPUI!
-------------------------------------

GPUI is a hybrid immediate and retained mode, GPU accelerated, UI framework
for Rust, designed to support a wide variety of applications.

### [§](#getting-started)Getting Started

GPUI is still in active development as we work on the Zed code editor, and is still pre-1.0. There will often be breaking changes between versions. You’ll also need to use the latest version of stable Rust and be on macOS or Linux. Add the following to your `Cargo.toml`:

```
gpui = { version = "*" }
```

* [Ownership and data flow](src/_ownership_and_data_flow.rs)

Everything in GPUI starts with an `Application`. You can create one with `Application::new()`, and kick off your application by passing a callback to `Application::run()`. Inside this callback, you can create a new window with `App::open_window()`, and register your first root view. See [gpui.rs](https://www.gpui.rs/) for a complete example.

#### [§](#dependencies)Dependencies

GPUI has various system dependencies that it needs in order to work.

##### [§](#macos)macOS

On macOS, GPUI uses Metal for rendering. In order to use Metal, you need to do the following:

* Install [Xcode](https://apps.apple.com/us/app/xcode/id497799835?mt=12) from the macOS App Store, or from the [Apple Developer](https://developer.apple.com/download/all/) website. Note this requires a developer account.

> Ensure you launch Xcode after installing, and install the macOS components, which is the default option.

* Install [Xcode command line tools](https://developer.apple.com/xcode/resources/)

  ```
  xcode-select --install
  ```
* Ensure that the Xcode command line tools are using your newly installed copy of Xcode:

  ```
  sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
  ```

### [§](#the-big-picture)The Big Picture

GPUI offers three different [registers](https://en.wikipedia.org/wiki/Register_(sociolinguistics)) depending on your needs:

* State management and communication with `Entity`’s. Whenever you need to store application state that communicates between different parts of your application, you’ll want to use GPUI’s entities. Entities are owned by GPUI and are only accessible through an owned smart pointer similar to an `Rc`. See the `app::context` module for more information.
* High level, declarative UI with views. All UI in GPUI starts with a view. A view is simply an `Entity` that can be rendered, by implementing the `Render` trait. At the start of each frame, GPUI will call this render method on the root view of a given window. Views build a tree of `elements`, lay them out and style them with a tailwind-style API, and then give them to GPUI to turn into pixels. See the `div` element for an all purpose swiss-army knife of rendering.
* Low level, imperative UI with Elements. Elements are the building blocks of UI in GPUI, and they provide a nice wrapper around an imperative API that provides as much flexibility and control as you need. Elements have total control over how they and their child elements are rendered and can be used for making efficient views into large lists, implement custom layouting for a code editor, and anything else you can think of. See the `element` module for more information.

Each of these registers has one or more corresponding contexts that can be accessed from all GPUI services. This context is your main interface to GPUI, and is used extensively throughout the framework.

### [§](#other-resources)Other Resources

In addition to the systems above, GPUI provides a range of smaller services that are useful for building complex applications:

* Actions are user-defined structs that are used for converting keystrokes into logical operations in your UI. Use this for implementing keyboard shortcuts, such as cmd-q. See the `action` module for more information.
* Platform services, such as `quit the app` or `open a URL` are available as methods on the `app::App`.
* An async executor that is integrated with the platform’s event loop. See the `executor` module for more information.,
* The `[gpui::test]` macro provides a convenient way to write tests for your GPUI applications. Tests also have their own kind of context, a `TestAppContext` which provides ways of simulating common platform input. See `app::test_context` and `test` modules for more details.

Currently, the best way to learn about these APIs is to read the Zed source code, ask us about it at a fireside hack, or drop a question in the [Zed Discord](https://zed.dev/community-links). We’re working on improving the documentation, creating more examples, and will be publishing more guides to GPUI on our [blog](https://zed.dev/blog).

Re-exports[§](#reexports)
-------------------------

`pub use http_client;`

Modules[§](#modules)
--------------------

[\_ownership\_and\_data\_flow](_ownership_and_data_flow/index.html "mod gpui::_ownership_and_data_flow")
:   In GPUI, every model or view in the application is actually owned by a single top-level object called the `App`. When a new entity or view is created (referred to collectively as *entities*), the application is given ownership of their state to enable their participation in a variety of app services and interaction with other entities.

[colors](colors/index.html "mod gpui::colors")
:   The default colors used by GPUI.

[inspector\_reflection](inspector_reflection/index.html "mod gpui::inspector_reflection")
:   Provides definitions used by `#[derive_inspector_reflection]`.

[prelude](prelude/index.html "mod gpui::prelude")
:   The GPUI prelude is a collection of traits and types that are widely used
    throughout the library. It is recommended to import this prelude into your
    application to avoid having to import each trait individually.

[styled\_reflection](styled_reflection/index.html "mod gpui::styled_reflection")
:   Implements function reflection

Macros[§](#macros)
------------------

[actions](macro.actions.html "macro gpui::actions")
:   Defines and registers unit structs that can be used as actions. For more complex data types, derive `Action`.

[border\_style\_methods](macro.border_style_methods.html "macro gpui::border_style_methods")
:   Generates methods for border styles.

[box\_shadow\_style\_methods](macro.box_shadow_style_methods.html "macro gpui::box_shadow_style_methods")
:   Generates methods for box shadow styles.

[cursor\_style\_methods](macro.cursor_style_methods.html "macro gpui::cursor_style_methods")
:   Generates methods for cursor styles.

[margin\_style\_methods](macro.margin_style_methods.html "macro gpui::margin_style_methods")
:   Generates methods for margin styles.

[overflow\_style\_methods](macro.overflow_style_methods.html "macro gpui::overflow_style_methods")
:   Generates methods for overflow styles.

[padding\_style\_methods](macro.padding_style_methods.html "macro gpui::padding_style_methods")
:   Generates methods for padding styles.

[position\_style\_methods](macro.position_style_methods.html "macro gpui::position_style_methods")
:   Generates methods for position styles.

[register\_action](macro.register_action.html "macro gpui::register_action")
:   This can be used to register an action with the GPUI runtime when you want to manually implement
    the `Action` trait. Typically you should use the `Action` derive macro or `actions!` macro
    instead.

[visibility\_style\_methods](macro.visibility_style_methods.html "macro gpui::visibility_style_methods")
:   Generates methods for visibility styles.

Structs[§](#structs)
--------------------

[Anchored](struct.Anchored.html "struct gpui::Anchored")
:   An anchored element that can be used to display UI that
    will avoid overflowing the window bounds.

[AnchoredState](struct.AnchoredState.html "struct gpui::AnchoredState")
:   The state that the anchored element element uses to track its children.

[Animation](struct.Animation.html "struct gpui::Animation")
:   An animation that can be applied to an element.

[AnimationElement](struct.AnimationElement.html "struct gpui::AnimationElement")
:   A GPUI element that applies an animation to another element

[AnyDrag](struct.AnyDrag.html "struct gpui::AnyDrag")
:   Contains state associated with an active drag operation, started by dragging an element
    within the window or by dragging into the app from the underlying platform.

[AnyElement](struct.AnyElement.html "struct gpui::AnyElement")
:   A dynamically typed element that can be used to store any element type.

[AnyEntity](struct.AnyEntity.html "struct gpui::AnyEntity")
:   A dynamically typed reference to a entity, which can be downcast into a `Entity<T>`.

[AnyImageCache](struct.AnyImageCache.html "struct gpui::AnyImageCache")
:   A dynamically typed image cache, which can be used to store any image cache

[AnyTooltip](struct.AnyTooltip.html "struct gpui::AnyTooltip")
:   Contains state associated with a tooltip. You’ll only need this struct if you’re implementing
    tooltip behavior on a custom element. Otherwise, use [Div::tooltip](struct.Interactivity.html#method.tooltip "method gpui::Interactivity::tooltip").

[AnyView](struct.AnyView.html "struct gpui::AnyView")
:   A dynamically-typed handle to a view, which can be downcast to a [Entity](struct.Entity.html "struct gpui::Entity") for a specific type.

[AnyWeakEntity](struct.AnyWeakEntity.html "struct gpui::AnyWeakEntity")
:   A type erased, weak reference to a entity.

[AnyWeakView](struct.AnyWeakView.html "struct gpui::AnyWeakView")
:   A weak, dynamically-typed view handle that does not prevent the view from being released.

[AnyWindowHandle](struct.AnyWindowHandle.html "struct gpui::AnyWindowHandle")
:   A handle to a window with any root view type, which can be downcast to a window with a specific root view type.

[App](struct.App.html "struct gpui::App")
:   Contains the state of the full application, and passed as a reference to a variety of callbacks.
    Other [Context](struct.Context.html "struct gpui::Context") derefs to this type.
    You need a reference to an `App` to access the state of a [Entity](struct.Entity.html "struct gpui::Entity").

[Application](struct.Application.html "struct gpui::Application")
:   A reference to a GPUI application, typically constructed in the `main` function of your app.
    You won’t interact with this type much outside of initial configuration and startup.

[ArenaClearNeeded](struct.ArenaClearNeeded.html "struct gpui::ArenaClearNeeded")
:   Returned when the element arena has been used and so must be cleared before the next draw.

[AsyncApp](struct.AsyncApp.html "struct gpui::AsyncApp")
:   An async-friendly version of [App](struct.App.html "struct gpui::App") with a static lifetime so it can be held across `await` points in async code.
    You’re provided with an instance when calling [App::spawn](struct.App.html#method.spawn "method gpui::App::spawn"), and you can also create one with [App::to\_async](struct.App.html#method.to_async "method gpui::App::to_async").
    Internally, this holds a weak reference to an `App`, so its methods are fallible to protect against cases where the [App](struct.App.html "struct gpui::App") is dropped.

[AsyncWindowContext](struct.AsyncWindowContext.html "struct gpui::AsyncWindowContext")
:   A cloneable, owned handle to the application context,
    composed with the window associated with the current task.

[Background](struct.Background.html "struct gpui::Background")
:   A background color, which can be either a solid color or a linear gradient.

[BackgroundExecutor](struct.BackgroundExecutor.html "struct gpui::BackgroundExecutor")
:   A pointer to the executor that is currently running,
    for spawning background tasks.

[BindingIndex](struct.BindingIndex.html "struct gpui::BindingIndex")
:   Index of a binding within a keymap.

[Boundary](struct.Boundary.html "struct gpui::Boundary")
:   A boundary between two lines of text.

[Bounds](struct.Bounds.html "struct gpui::Bounds")
:   Represents a rectangular area in a 2D space with an origin point and a size.

[BoundsRefinement](struct.BoundsRefinement.html "struct gpui::BoundsRefinement")
:   A refinable version of [`#ident`], see that documentation for details.

[BoxShadow](struct.BoxShadow.html "struct gpui::BoxShadow")
:   The possible values of the box-shadow property

[Canvas](struct.Canvas.html "struct gpui::Canvas")
:   A canvas element, meant for accessing the low level paint API without defining a whole
    custom element

[Capslock](struct.Capslock.html "struct gpui::Capslock")
:   The state of the capslock key at some point in time

[Cascade](struct.Cascade.html "struct gpui::Cascade")
:   A cascade of refinements that can be merged in priority order.

[CascadeSlot](struct.CascadeSlot.html "struct gpui::CascadeSlot")
:   A handle to a specific slot in a cascade.

[ClipboardItem](struct.ClipboardItem.html "struct gpui::ClipboardItem")
:   A clipboard item that should be copied to the clipboard

[ClipboardString](struct.ClipboardString.html "struct gpui::ClipboardString")
:   A clipboard item that should be copied to the clipboard

[ContentMask](struct.ContentMask.html "struct gpui::ContentMask")
:   Indicates which region of the window is visible. Content falling outside of this mask will not be
    rendered. Currently, only rectangular content masks are supported, but we give the mask its own type
    to leave room to support more complex shapes in the future.

[Context](struct.Context.html "struct gpui::Context")
:   The app context, with specialized behavior for the given entity.

[ContextEntry](struct.ContextEntry.html "struct gpui::ContextEntry")
:   An entry in a KeyContext

[Corners](struct.Corners.html "struct gpui::Corners")
:   Represents the corners of a box in a 2D space, such as border radius.

[CornersRefinement](struct.CornersRefinement.html "struct gpui::CornersRefinement")
:   A refinable version of [`#ident`], see that documentation for details.

[DebugBelow](struct.DebugBelow.html "struct gpui::DebugBelow")
:   Use this struct for interfacing with the ‘debug\_below’ styling from your own elements.
    If a parent element has this style set on it, then this struct will be set as a global in
    GPUI.

[DecorationRun](struct.DecorationRun.html "struct gpui::DecorationRun")
:   Set the text decoration for a run of text.

[Deferred](struct.Deferred.html "struct gpui::Deferred")
:   An element which delays the painting of its child until after all of
    its ancestors, while keeping its layout as part of the current element tree.

[DeferredScrollToItem](struct.DeferredScrollToItem.html "struct gpui::DeferredScrollToItem")

[DevicePixels](struct.DevicePixels.html "struct gpui::DevicePixels")
:   Represents physical pixels on the display.

[DismissEvent](struct.DismissEvent.html "struct gpui::DismissEvent")
:   Emitted by implementers of [`ManagedView`](trait.ManagedView.html "trait gpui::ManagedView") to indicate the view should be dismissed, such as when a view is presented as a modal.

[DisplayId](struct.DisplayId.html "struct gpui::DisplayId")
:   An opaque identifier for a hardware display

[Div](struct.Div.html "struct gpui::Div")
:   A [`Div`](struct.Div.html "struct gpui::Div") element, the all-in-one element for building complex UIs in GPUI

[DivFrameState](struct.DivFrameState.html "struct gpui::DivFrameState")
:   A frame state for a `Div` element, which contains layout IDs for its children.

[DivInspectorState](struct.DivInspectorState.html "struct gpui::DivInspectorState")
:   Interactivity state displayed an manipulated in the inspector.

[DragMoveEvent](struct.DragMoveEvent.html "struct gpui::DragMoveEvent")
:   An event for when a drag is moving over this element, with the given state type.

[Drawable](struct.Drawable.html "struct gpui::Drawable")
:   A wrapper around an implementer of [`Element`](trait.Element.html "trait gpui::Element") that allows it to be drawn in a window.

[DummyKeyboardMapper](struct.DummyKeyboardMapper.html "struct gpui::DummyKeyboardMapper")
:   A dummy implementation of the platform keyboard mapper

[Edges](struct.Edges.html "struct gpui::Edges")
:   Represents the edges of a box in a 2D space, such as padding or margin.

[EdgesRefinement](struct.EdgesRefinement.html "struct gpui::EdgesRefinement")
:   A refinable version of [`#ident`], see that documentation for details.

[ElementClickedState](struct.ElementClickedState.html "struct gpui::ElementClickedState")
:   Whether or not the element or a group that contains it is clicked by the mouse.

[ElementInputHandler](struct.ElementInputHandler.html "struct gpui::ElementInputHandler")
:   The canonical implementation of [`crate::PlatformInputHandler`]. Call [`Window::handle_input`](struct.Window.html#method.handle_input "method gpui::Window::handle_input")
    with an instance during your element’s paint.

[Empty](struct.Empty.html "struct gpui::Empty")
:   The empty element, which renders nothing.

[EmptyView](struct.EmptyView.html "struct gpui::EmptyView")
:   A view that renders nothing

[Entity](struct.Entity.html "struct gpui::Entity")
:   A strong, well-typed reference to a struct which is managed
    by GPUI

[EntityId](struct.EntityId.html "struct gpui::EntityId")
:   A unique identifier for a entity across the application.

[ExternalPaths](struct.ExternalPaths.html "struct gpui::ExternalPaths")
:   A collection of paths from the platform, such as from a file drop.

[FallbackPromptRenderer](struct.FallbackPromptRenderer.html "struct gpui::FallbackPromptRenderer")
:   The default GPUI fallback for rendering prompts, when the platform doesn’t support it.

[FillOptions](struct.FillOptions.html "struct gpui::FillOptions")
:   Parameters for the fill tessellator.

[FocusHandle](struct.FocusHandle.html "struct gpui::FocusHandle")
:   A handle which can be used to track and manipulate the focused element in a window.

[FocusId](struct.FocusId.html "struct gpui::FocusId")
:   A globally unique identifier for a focusable element.

[FocusOutEvent](struct.FocusOutEvent.html "struct gpui::FocusOutEvent")
:   This is provided when subscribing for `Context::on_focus_out` events.

[Font](struct.Font.html "struct gpui::Font")
:   The configuration details for identifying a specific font.

[FontFallbacks](struct.FontFallbacks.html "struct gpui::FontFallbacks")
:   The fallback fonts that can be configured for a given font.
    Fallback fonts family names are stored here.

[FontFamilyId](struct.FontFamilyId.html "struct gpui::FontFamilyId")
:   An opaque identifier for a specific font family.

[FontFeatures](struct.FontFeatures.html "struct gpui::FontFeatures")
:   The OpenType features that can be configured for a given font.

[FontId](struct.FontId.html "struct gpui::FontId")
:   An opaque identifier for a specific font.

[FontMetrics](struct.FontMetrics.html "struct gpui::FontMetrics")
:   A struct for storing font metrics.
    It is used to define the measurements of a typeface.

[FontRun](struct.FontRun.html "struct gpui::FontRun")
:   A run of text with a single font.

[FontWeight](struct.FontWeight.html "struct gpui::FontWeight")
:   The degree of blackness or stroke thickness of a font. This value ranges from 100.0 to 900.0,
    with 400.0 as normal.

[ForegroundExecutor](struct.ForegroundExecutor.html "struct gpui::ForegroundExecutor")
:   A pointer to the executor that is currently running,
    for spawning tasks on the main thread.

[GlobalElementId](struct.GlobalElementId.html "struct gpui::GlobalElementId")
:   A globally unique identifier for an element, used to track state across frames.

[GlyphId](struct.GlyphId.html "struct gpui::GlyphId")
:   An identifier for a specific glyph, as returned by [`WindowTextSystem::layout_line`](struct.WindowTextSystem.html#method.layout_line "method gpui::WindowTextSystem::layout_line").

[GpuSpecs](struct.GpuSpecs.html "struct gpui::GpuSpecs")
:   Information about the GPU GPUI is running on.

[GpuiBorrow](struct.GpuiBorrow.html "struct gpui::GpuiBorrow")
:   A mutable reference to an entity owned by GPUI

[GridLocation](struct.GridLocation.html "struct gpui::GridLocation")
:   A location in a grid layout.

[GroupStyle](struct.GroupStyle.html "struct gpui::GroupStyle")
:   The styling information for a given group.

[HighlightStyle](struct.HighlightStyle.html "struct gpui::HighlightStyle")
:   A highlight style to apply, similar to a `TextStyle` except
    for a single font, uniformly sized and spaced text.

[Hitbox](struct.Hitbox.html "struct gpui::Hitbox")
:   A rectangular region that potentially blocks hitboxes inserted prior.
    See [Window::insert\_hitbox](struct.Window.html#method.insert_hitbox "method gpui::Window::insert_hitbox") for more details.

[HitboxId](struct.HitboxId.html "struct gpui::HitboxId")
:   An identifier for a [Hitbox](struct.Hitbox.html "struct gpui::Hitbox") which also includes [HitboxBehavior](enum.HitboxBehavior.html "enum gpui::HitboxBehavior").

[Hsla](struct.Hsla.html "struct gpui::Hsla")
:   An HSLA color

[Image](struct.Image.html "struct gpui::Image")
:   An image, with a format and certain bytes

[ImageCacheElement](struct.ImageCacheElement.html "struct gpui::ImageCacheElement")
:   An image cache element.

[ImageFormatIter](struct.ImageFormatIter.html "struct gpui::ImageFormatIter")
:   An iterator over the variants of [ImageFormat](enum.ImageFormat.html "enum gpui::ImageFormat")

[ImageId](struct.ImageId.html "struct gpui::ImageId")
:   A unique identifier for the image cache

[ImageStyle](struct.ImageStyle.html "struct gpui::ImageStyle")
:   The style of an image element.

[Img](struct.Img.html "struct gpui::Img")
:   An image element.

[ImgLayoutState](struct.ImgLayoutState.html "struct gpui::ImgLayoutState")
:   The image layout state between frames

[Inspector](struct.Inspector.html "struct gpui::Inspector")
:   Manages inspector state - which element is currently selected and whether the inspector is
    in picking mode.

[InspectorElementId](struct.InspectorElementId.html "struct gpui::InspectorElementId")
:   A unique identifier for an element that can be inspected.

[InspectorElementPath](struct.InspectorElementPath.html "struct gpui::InspectorElementPath")
:   `GlobalElementId` qualified by source location of element construction.

[InteractiveElementState](struct.InteractiveElementState.html "struct gpui::InteractiveElementState")
:   The per-frame state of an interactive element. Used for tracking stateful interactions like clicks
    and scroll offsets.

[InteractiveText](struct.InteractiveText.html "struct gpui::InteractiveText")
:   A text element that can be interacted with.

[Interactivity](struct.Interactivity.html "struct gpui::Interactivity")
:   The interactivity struct. Powers all of the general-purpose
    interactivity in the `Div` element.

[InvalidKeystrokeError](struct.InvalidKeystrokeError.html "struct gpui::InvalidKeystrokeError")
:   Error type for `Keystroke::parse`. This is used instead of `anyhow::Error` so that Zed can use
    markdown to display it.

[ItemSize](struct.ItemSize.html "struct gpui::ItemSize")
:   The size of the item and its contents.

[KeyBinding](struct.KeyBinding.html "struct gpui::KeyBinding")
:   A keybinding and its associated metadata, from the keymap.

[KeyBindingMetaIndex](struct.KeyBindingMetaIndex.html "struct gpui::KeyBindingMetaIndex")
:   A unique identifier for retrieval of metadata associated with a key binding.
    Intended to be used as an index or key into a user-defined store of metadata
    associated with the binding, such as the source of the binding.

[KeyContext](struct.KeyContext.html "struct gpui::KeyContext")
:   A datastructure for resolving whether an action should be dispatched
    at this point in the element tree. Contains a set of identifiers
    and/or key value pairs representing the current context for the
    keymap.

[KeyDownEvent](struct.KeyDownEvent.html "struct gpui::KeyDownEvent")
:   The key down event equivalent for the platform.

[KeyUpEvent](struct.KeyUpEvent.html "struct gpui::KeyUpEvent")
:   The key up event equivalent for the platform.

[KeybindingKeystroke](struct.KeybindingKeystroke.html "struct gpui::KeybindingKeystroke")
:   Represents a keystroke that can be used in keybindings and displayed to the user.

[KeyboardClickEvent](struct.KeyboardClickEvent.html "struct gpui::KeyboardClickEvent")
:   A click event that was generated by a keyboard button being pressed and released.

[Keymap](struct.Keymap.html "struct gpui::Keymap")
:   A collection of key bindings for the user’s application.

[KeymapVersion](struct.KeymapVersion.html "struct gpui::KeymapVersion")
:   An opaque identifier of which version of the keymap is currently active.
    The keymap’s version is changed whenever bindings are added or removed.

[Keystroke](struct.Keystroke.html "struct gpui::Keystroke")
:   A keystroke and associated metadata generated by the platform

[KeystrokeEvent](struct.KeystrokeEvent.html "struct gpui::KeystrokeEvent")
:   A keystroke event, and potentially the associated action

[LayoutId](struct.LayoutId.html "struct gpui::LayoutId")
:   A unique identifier for a layout node, generated when requesting a layout from Taffy

[LineLayout](struct.LineLayout.html "struct gpui::LineLayout")
:   A laid out and styled line of text

[LineWrapper](struct.LineWrapper.html "struct gpui::LineWrapper")
:   The GPUI line wrapper, used to wrap lines of text to a given width.

[LineWrapperHandle](struct.LineWrapperHandle.html "struct gpui::LineWrapperHandle")
:   A handle into the text system, which can be used to compute the wrapped layout of text

[LinearColorStop](struct.LinearColorStop.html "struct gpui::LinearColorStop")
:   A color stop in a linear gradient.

[List](struct.List.html "struct gpui::List")
:   A list element

[ListOffset](struct.ListOffset.html "struct gpui::ListOffset")
:   An offset into the list’s items, in terms of the item index and the number
    of pixels off the top left of the item.

[ListPrepaintState](struct.ListPrepaintState.html "struct gpui::ListPrepaintState")
:   Frame state used by the [List](struct.List.html "struct gpui::List") element after layout.

[ListScrollEvent](struct.ListScrollEvent.html "struct gpui::ListScrollEvent")
:   A scroll event that has been converted to be in terms of the list’s items.

[ListState](struct.ListState.html "struct gpui::ListState")
:   The list state that views must hold on behalf of the list element.

[Menu](struct.Menu.html "struct gpui::Menu")
:   A menu of the application, either a main menu or a submenu

[Modifiers](struct.Modifiers.html "struct gpui::Modifiers")
:   The state of the modifier keys at some point in time

[ModifiersChangedEvent](struct.ModifiersChangedEvent.html "struct gpui::ModifiersChangedEvent")
:   The modifiers changed event equivalent for the platform.

[MouseClickEvent](struct.MouseClickEvent.html "struct gpui::MouseClickEvent")
:   A click event, generated when a mouse button is pressed and released.

[MouseDownEvent](struct.MouseDownEvent.html "struct gpui::MouseDownEvent")
:   A mouse down event from the platform

[MouseExitEvent](struct.MouseExitEvent.html "struct gpui::MouseExitEvent")
:   A mouse exit event from the platform, generated when the mouse leaves the window.

[MouseMoveEvent](struct.MouseMoveEvent.html "struct gpui::MouseMoveEvent")
:   A mouse move event from the platform

[MouseUpEvent](struct.MouseUpEvent.html "struct gpui::MouseUpEvent")
:   A mouse up event from the platform

[NoAction](struct.NoAction.html "struct gpui::NoAction")
:   Action with special handling which unbinds the keybinding this is associated with,
    if it is the highest precedence match.

[OsMenu](struct.OsMenu.html "struct gpui::OsMenu")
:   OS menus are menus that are recognized by the operating system
    This allows the operating system to provide specialized items for
    these menus

[OwnedMenu](struct.OwnedMenu.html "struct gpui::OwnedMenu")
:   A menu of the application, either a main menu or a submenu

[OwnedOsMenu](struct.OwnedOsMenu.html "struct gpui::OwnedOsMenu")
:   OS menus are menus that are recognized by the operating system
    This allows the operating system to provide specialized items for
    these menus

[PaintQuad](struct.PaintQuad.html "struct gpui::PaintQuad")
:   A rectangle to be rendered in the window at the given position and size.
    Passed as an argument [`Window::paint_quad`](struct.Window.html#method.paint_quad "method gpui::Window::paint_quad").

[Path](struct.Path.html "struct gpui::Path")
:   A line made up of a series of vertices and control points.

[PathBuilder](struct.PathBuilder.html "struct gpui::PathBuilder")
:   A [`Path`](struct.Path.html "struct gpui::Path") builder.

[PathPromptOptions](struct.PathPromptOptions.html "struct gpui::PathPromptOptions")
:   The options that can be configured for a file dialog prompt

[Percentage](struct.Percentage.html "struct gpui::Percentage")
:   A type representing a percentage value.

[Pixels](struct.Pixels.html "struct gpui::Pixels")
:   Represents a length in pixels, the base unit of measurement in the UI framework.

[Point](struct.Point.html "struct gpui::Point")
:   Describes a location in a 2D cartesian space.

[PointRefinement](struct.PointRefinement.html "struct gpui::PointRefinement")
:   A refinable version of [`#ident`], see that documentation for details.

[PromptHandle](struct.PromptHandle.html "struct gpui::PromptHandle")
:   A handle to a prompt that can be used to interact with it.

[PromptResponse](struct.PromptResponse.html "struct gpui::PromptResponse")
:   The event emitted when a prompt’s option is selected.
    The usize is the index of the selected option, from the actions
    passed to the prompt.

[Radians](struct.Radians.html "struct gpui::Radians")
:   Represents an angle in Radians

[Rems](struct.Rems.html "struct gpui::Rems")
:   Represents a length in rems, a unit based on the font-size of the window, which can be assigned with [`Window::set_rem_size`](struct.Window.html#method.set_rem_size "method gpui::Window::set_rem_size").

[RenderImage](struct.RenderImage.html "struct gpui::RenderImage")
:   A cached and processed image, in BGRA format

[RenderablePromptHandle](struct.RenderablePromptHandle.html "struct gpui::RenderablePromptHandle")
:   A prompt handle capable of being rendered in a window.

[Reservation](struct.Reservation.html "struct gpui::Reservation")
:   Returned by [Context::reserve\_entity](struct.Context.html#method.reserve_entity "method gpui::Context::reserve_entity") to later be passed to [Context::insert\_entity](struct.Context.html#method.insert_entity "method gpui::Context::insert_entity").
    Allows you to obtain the [EntityId](struct.EntityId.html "struct gpui::EntityId") for a entity before it is created.

[RetainAllImageCache](struct.RetainAllImageCache.html "struct gpui::RetainAllImageCache")
:   An implementation of ImageCache, that uses an LRU caching strategy to unload images when the cache is full

[RetainAllImageCacheProvider](struct.RetainAllImageCacheProvider.html "struct gpui::RetainAllImageCacheProvider")
:   A provider struct for creating a retain-all image cache inline

[Rgba](struct.Rgba.html "struct gpui::Rgba")
:   An RGBA color

[ScaledPixels](struct.ScaledPixels.html "struct gpui::ScaledPixels")
:   Represents scaled pixels that take into account the device’s scale factor.

[Scope](struct.Scope.html "struct gpui::Scope")
:   Scope manages a set of tasks that are enqueued and waited on together. See [`BackgroundExecutor::scoped`](struct.BackgroundExecutor.html#method.scoped "method gpui::BackgroundExecutor::scoped").

[ScreenCaptureFrame](struct.ScreenCaptureFrame.html "struct gpui::ScreenCaptureFrame")
:   A frame of video captured from a screen.

[ScrollAnchor](struct.ScrollAnchor.html "struct gpui::ScrollAnchor")
:   Represents an element that can be scrolled *to* in its parent element.
    Contrary to [ScrollHandle::scroll\_to\_active\_item](struct.ScrollHandle.html#method.scroll_to_active_item "method gpui::ScrollHandle::scroll_to_active_item"), an anchored element does not have to be an immediate child of the parent.

[ScrollHandle](struct.ScrollHandle.html "struct gpui::ScrollHandle")
:   A handle to the scrollable aspects of an element.
    Used for accessing scroll state, like the current scroll offset,
    and for mutating the scroll state, like scrolling to a specific child.

[ScrollWheelEvent](struct.ScrollWheelEvent.html "struct gpui::ScrollWheelEvent")
:   A mouse wheel event from the platform

[SemanticVersion](struct.SemanticVersion.html "struct gpui::SemanticVersion")
:   A [semantic version](https://semver.org/) number.

[ShapedGlyph](struct.ShapedGlyph.html "struct gpui::ShapedGlyph")
:   A single glyph, ready to paint.

[ShapedLine](struct.ShapedLine.html "struct gpui::ShapedLine")
:   A line of text that has been shaped and decorated.

[ShapedRun](struct.ShapedRun.html "struct gpui::ShapedRun")
:   A run of text that has been shaped .

[SharedString](struct.SharedString.html "struct gpui::SharedString")
:   A shared string is an immutable string that can be cheaply cloned in GPUI
    tasks. Essentially an abstraction over an `Arc<str>` and `&'static str`,

[SharedUri](struct.SharedUri.html "struct gpui::SharedUri")
:   A [`SharedString`](struct.SharedString.html "struct gpui::SharedString") containing a URI.

[Size](struct.Size.html "struct gpui::Size")
:   A structure representing a two-dimensional size with width and height in a given unit.

[SizeRefinement](struct.SizeRefinement.html "struct gpui::SizeRefinement")
:   A refinable version of [`#ident`], see that documentation for details.

[SourceMetadata](struct.SourceMetadata.html "struct gpui::SourceMetadata")
:   Metadata for a given [ScreenCaptureSource](trait.ScreenCaptureSource.html "trait gpui::ScreenCaptureSource")

[Stateful](struct.Stateful.html "struct gpui::Stateful")
:   A wrapper around an element that can store state, produced after assigning an ElementId.

[StrikethroughStyle](struct.StrikethroughStyle.html "struct gpui::StrikethroughStyle")
:   The properties that can be applied to a strikethrough.

[StrikethroughStyleRefinement](struct.StrikethroughStyleRefinement.html "struct gpui::StrikethroughStyleRefinement")
:   A refinable version of [`#ident`], see that documentation for details.

[StrokeOptions](struct.StrokeOptions.html "struct gpui::StrokeOptions")
:   Parameters for the tessellator.

[Style](struct.Style.html "struct gpui::Style")
:   The CSS styling that can be applied to an element via the `Styled` trait

[StyleRefinement](struct.StyleRefinement.html "struct gpui::StyleRefinement")
:   A refinable version of [`#ident`], see that documentation for details.

[StyledText](struct.StyledText.html "struct gpui::StyledText")
:   Renders text with runs of different styles.

[Subscription](struct.Subscription.html "struct gpui::Subscription")
:   A handle to a subscription created by GPUI. When dropped, the subscription
    is cancelled and the callback will no longer be invoked.

[Surface](struct.Surface.html "struct gpui::Surface")
:   A surface element.

[Svg](struct.Svg.html "struct gpui::Svg")
:   An SVG element.

[SystemWindowTabController](struct.SystemWindowTabController.html "struct gpui::SystemWindowTabController")
:   A controller for managing window tabs.

[Task](struct.Task.html "struct gpui::Task")
:   Task is a primitive that allows work to happen in the background.

[TaskLabel](struct.TaskLabel.html "struct gpui::TaskLabel")
:   A task label is an opaque identifier that you can use to
    refer to a task in tests.

[TextLayout](struct.TextLayout.html "struct gpui::TextLayout")
:   The Layout for TextElement. This can be used to map indices to pixels and vice versa.

[TextRun](struct.TextRun.html "struct gpui::TextRun")
:   A styled run of text, for use in [`crate::TextLayout`](struct.TextLayout.html "struct gpui::TextLayout").

[TextStyle](struct.TextStyle.html "struct gpui::TextStyle")
:   The properties that can be used to style text in GPUI

[TextStyleRefinement](struct.TextStyleRefinement.html "struct gpui::TextStyleRefinement")
:   A refinable version of [`#ident`], see that documentation for details.

[TextSystem](struct.TextSystem.html "struct gpui::TextSystem")
:   The GPUI text rendering sub system.

[Tiling](struct.Tiling.html "struct gpui::Tiling")
:   A type to describe which sides of the window are currently tiled in some way

[Timeout](struct.Timeout.html "struct gpui::Timeout")
:   Error returned by with\_timeout when the timeout duration elapsed before the future resolved

[Timer](struct.Timer.html "struct gpui::Timer")
:   A future or stream that emits timed events.

[TitlebarOptions](struct.TitlebarOptions.html "struct gpui::TitlebarOptions")
:   The options that can be configured for a window’s titlebar

[TooltipId](struct.TooltipId.html "struct gpui::TooltipId")
:   An identifier for a tooltip.

[Transformation](struct.Transformation.html "struct gpui::Transformation")
:   A transformation to apply to an SVG element.

[TransformationMatrix](struct.TransformationMatrix.html "struct gpui::TransformationMatrix")
:   A data type representing a 2 dimensional transformation that can be applied to an element.

[UTF16Selection](struct.UTF16Selection.html "struct gpui::UTF16Selection")
:   A struct representing a selection in a text buffer, in UTF16 characters.
    This is different from a range because the head may be before the tail.

[UnderlineStyle](struct.UnderlineStyle.html "struct gpui::UnderlineStyle")
:   The properties that can be applied to an underline.

[UnderlineStyleRefinement](struct.UnderlineStyleRefinement.html "struct gpui::UnderlineStyleRefinement")
:   A refinable version of [`#ident`], see that documentation for details.

[UniformList](struct.UniformList.html "struct gpui::UniformList")
:   A list element for efficiently laying out and displaying a list of uniform-height elements.

[UniformListFrameState](struct.UniformListFrameState.html "struct gpui::UniformListFrameState")
:   Frame state used by the [UniformList](struct.UniformList.html "struct gpui::UniformList").

[UniformListScrollHandle](struct.UniformListScrollHandle.html "struct gpui::UniformListScrollHandle")
:   A handle for controlling the scroll position of a uniform list.
    This should be stored in your view and passed to the uniform\_list on each frame.

[UniformListScrollState](struct.UniformListScrollState.html "struct gpui::UniformListScrollState")

[WeakEntity](struct.WeakEntity.html "struct gpui::WeakEntity")
:   A weak reference to a entity of the given type.

[WeakFocusHandle](struct.WeakFocusHandle.html "struct gpui::WeakFocusHandle")
:   A weak reference to a focus handle.

[Window](struct.Window.html "struct gpui::Window")
:   Holds the state for a specific window.

[WindowControls](struct.WindowControls.html "struct gpui::WindowControls")
:   What window controls this platform supports

[WindowHandle](struct.WindowHandle.html "struct gpui::WindowHandle")
:   A handle to a window with a specific root view type.
    Note that this does not keep the window alive on its own.

[WindowId](struct.WindowId.html "struct gpui::WindowId")
:   A unique identifier for a window.

[WindowOptions](struct.WindowOptions.html "struct gpui::WindowOptions")
:   The variables that can be configured when creating a new window

[WindowTextSystem](struct.WindowTextSystem.html "struct gpui::WindowTextSystem")
:   The GPUI text layout subsystem.

[WrapBoundary](struct.WrapBoundary.html "struct gpui::WrapBoundary")
:   A boundary at which a line was wrapped

[WrappedLine](struct.WrappedLine.html "struct gpui::WrappedLine")
:   A line of text that has been shaped, decorated, and wrapped by the text layout system.

[WrappedLineLayout](struct.WrappedLineLayout.html "struct gpui::WrappedLineLayout")
:   A line of text that has been wrapped to fit a given width

Enums[§](#enums)
----------------

[AbsoluteLength](enum.AbsoluteLength.html "enum gpui::AbsoluteLength")
:   Represents an absolute length in pixels or rems.

[ActionBuildError](enum.ActionBuildError.html "enum gpui::ActionBuildError")
:   Error type for `Keystroke::parse`. This is used instead of `anyhow::Error` so that Zed can use
    markdown to display it.

[AlignContent](enum.AlignContent.html "enum gpui::AlignContent")
:   Sets the distribution of space between and around content items
    For Flexbox it controls alignment in the cross axis
    For Grid it controls alignment in the block axis

[AlignItems](enum.AlignItems.html "enum gpui::AlignItems")
:   Used to control how child nodes are aligned.
    For Flexbox it controls alignment in the cross axis
    For Grid it controls alignment in the block axis

[AnchoredFitMode](enum.AnchoredFitMode.html "enum gpui::AnchoredFitMode")
:   Which algorithm to use when fitting the anchored element to be inside the window.

[AnchoredPositionMode](enum.AnchoredPositionMode.html "enum gpui::AnchoredPositionMode")
:   Which algorithm to use when positioning the anchored element.

[ArcCow](enum.ArcCow.html "enum gpui::ArcCow")

[AssetLogger](enum.AssetLogger.html "enum gpui::AssetLogger")
:   An asset Loader which logs the [`Err`](https://doc.rust-lang.org/nightly/core/result/enum.Result.html#variant.Err "variant core::result::Result::Err") variant of a [`Result`](https://doc.rust-lang.org/nightly/core/result/enum.Result.html "enum core::result::Result") during loading

[AvailableSpace](enum.AvailableSpace.html "enum gpui::AvailableSpace")
:   The space available for an element to be laid out in

[Axis](enum.Axis.html "enum gpui::Axis")
:   Axis in a 2D cartesian space.

[BorderStyle](enum.BorderStyle.html "enum gpui::BorderStyle")
:   The style of a border.

[ClickEvent](enum.ClickEvent.html "enum gpui::ClickEvent")
:   A click event, generated when a mouse button or keyboard button is pressed and released.

[ClipboardEntry](enum.ClipboardEntry.html "enum gpui::ClipboardEntry")
:   Either a ClipboardString or a ClipboardImage

[ColorSpace](enum.ColorSpace.html "enum gpui::ColorSpace")
:   A color space for color interpolation.

[Corner](enum.Corner.html "enum gpui::Corner")
:   Identifies a corner of a 2d box.

[CursorStyle](enum.CursorStyle.html "enum gpui::CursorStyle")
:   The style of the cursor (pointer)

[Decorations](enum.Decorations.html "enum gpui::Decorations")
:   A type to describe how this window is currently configured

[DefiniteLength](enum.DefiniteLength.html "enum gpui::DefiniteLength")
:   A non-auto length that can be defined in pixels, rems, or percent of parent.

[DispatchPhase](enum.DispatchPhase.html "enum gpui::DispatchPhase")
:   Represents the two different phases when dispatching events.

[Display](enum.Display.html "enum gpui::Display")
:   Sets the layout used for the children of this node

[ElementId](enum.ElementId.html "enum gpui::ElementId")
:   An identifier for an [`Element`](trait.Element.html "trait gpui::Element").

[FileDropEvent](enum.FileDropEvent.html "enum gpui::FileDropEvent")
:   A file drop event from the platform, generated when files are dragged and dropped onto the window.

[Fill](enum.Fill.html "enum gpui::Fill")
:   The kinds of fill that can be applied to a shape.

[FillRule](enum.FillRule.html "enum gpui::FillRule")
:   The fill rule defines how to determine what is inside and what is outside of the shape.

[FlexDirection](enum.FlexDirection.html "enum gpui::FlexDirection")
:   The direction of the flexbox layout main axis.

[FlexWrap](enum.FlexWrap.html "enum gpui::FlexWrap")
:   Controls whether flex items are forced onto one line or can wrap onto multiple lines.

[FontStyle](enum.FontStyle.html "enum gpui::FontStyle")
:   Allows italic or oblique faces to be selected.

[GridPlacement](enum.GridPlacement.html "enum gpui::GridPlacement")
:   The placement of an item within a grid layout’s column or row.

[HitboxBehavior](enum.HitboxBehavior.html "enum gpui::HitboxBehavior")
:   How the hitbox affects mouse behavior.

[ImageAssetLoader](enum.ImageAssetLoader.html "enum gpui::ImageAssetLoader")
:   An image loader for the GPUI asset system

[ImageCacheError](enum.ImageCacheError.html "enum gpui::ImageCacheError")
:   An error that can occur when interacting with the image cache.

[ImageCacheItem](enum.ImageCacheItem.html "enum gpui::ImageCacheItem")
:   An image cache item

[ImageFormat](enum.ImageFormat.html "enum gpui::ImageFormat")
:   One of the editor’s supported image formats (e.g. PNG, JPEG) - used when dealing with images in the clipboard

[ImageSource](enum.ImageSource.html "enum gpui::ImageSource")
:   A source of image content.

[KeyBindingContextPredicate](enum.KeyBindingContextPredicate.html "enum gpui::KeyBindingContextPredicate")
:   A datastructure for resolving whether an action should be dispatched
    Representing a small language for describing which contexts correspond
    to which actions.

[KeyboardButton](enum.KeyboardButton.html "enum gpui::KeyboardButton")
:   An enum representing the keyboard button that was pressed for a click event.

[Length](enum.Length.html "enum gpui::Length")
:   A length that can be defined in pixels, rems, percent of parent, or auto.

[LineFragment](enum.LineFragment.html "enum gpui::LineFragment")
:   A fragment of a line that can be wrapped.

[ListAlignment](enum.ListAlignment.html "enum gpui::ListAlignment")
:   Whether the list is scrolling from top to bottom or bottom to top.

[ListHorizontalSizingBehavior](enum.ListHorizontalSizingBehavior.html "enum gpui::ListHorizontalSizingBehavior")
:   The horizontal sizing behavior to apply during layout.

[ListMeasuringBehavior](enum.ListMeasuringBehavior.html "enum gpui::ListMeasuringBehavior")
:   The measuring behavior to apply during layout.

[ListSizingBehavior](enum.ListSizingBehavior.html "enum gpui::ListSizingBehavior")
:   The sizing behavior to apply during layout.

[MenuItem](enum.MenuItem.html "enum gpui::MenuItem")
:   The different kinds of items that can be in a menu

[MouseButton](enum.MouseButton.html "enum gpui::MouseButton")
:   An enum representing the mouse button that was pressed.

[NavigationDirection](enum.NavigationDirection.html "enum gpui::NavigationDirection")
:   A navigation direction, such as back or forward.

[ObjectFit](enum.ObjectFit.html "enum gpui::ObjectFit")
:   How to fit the image into the bounds of the element.

[OsAction](enum.OsAction.html "enum gpui::OsAction")
:   OS actions are actions that are recognized by the operating system
    This allows the operating system to provide specialized behavior for
    these actions

[Overflow](enum.Overflow.html "enum gpui::Overflow")
:   How children overflowing their container should affect layout

[OwnedMenuItem](enum.OwnedMenuItem.html "enum gpui::OwnedMenuItem")
:   The different kinds of items that can be in a menu

[PathStyle](enum.PathStyle.html "enum gpui::PathStyle")
:   Style of the PathBuilder

[PlatformInput](enum.PlatformInput.html "enum gpui::PlatformInput")
:   An enum corresponding to all kinds of platform input events.

[Position](enum.Position.html "enum gpui::Position")
:   The positioning strategy for this item.

[PromptButton](enum.PromptButton.html "enum gpui::PromptButton")
:   Prompt Button

[PromptLevel](enum.PromptLevel.html "enum gpui::PromptLevel")
:   What kind of prompt styling to show

[ResizeEdge](enum.ResizeEdge.html "enum gpui::ResizeEdge")
:   Which part of the window to resize

[Resource](enum.Resource.html "enum gpui::Resource")
:   An enum representing

[ScrollDelta](enum.ScrollDelta.html "enum gpui::ScrollDelta")
:   The scroll delta for a scroll wheel event.

[ScrollStrategy](enum.ScrollStrategy.html "enum gpui::ScrollStrategy")
:   Where to place the element scrolled to.

[SurfaceSource](enum.SurfaceSource.html "enum gpui::SurfaceSource")
:   A source of a surface’s content.

[SystemMenuType](enum.SystemMenuType.html "enum gpui::SystemMenuType")
:   The type of system menu

[TextAlign](enum.TextAlign.html "enum gpui::TextAlign")
:   How to align text within the element

[TextOverflow](enum.TextOverflow.html "enum gpui::TextOverflow")
:   How to truncate text that overflows the width of the element

[TouchPhase](enum.TouchPhase.html "enum gpui::TouchPhase")
:   The phase of a touch motion event.
    Based on the winit enum of the same name.

[Visibility](enum.Visibility.html "enum gpui::Visibility")
:   The value of the visibility property, similar to the CSS property `visibility`

[WhiteSpace](enum.WhiteSpace.html "enum gpui::WhiteSpace")
:   How to handle whitespace in text

[WindowAppearance](enum.WindowAppearance.html "enum gpui::WindowAppearance")
:   The appearance of the window, as defined by the operating system.

[WindowBackgroundAppearance](enum.WindowBackgroundAppearance.html "enum gpui::WindowBackgroundAppearance")
:   The appearance of the background of the window itself, when there is
    no content or the content is transparent.

[WindowBounds](enum.WindowBounds.html "enum gpui::WindowBounds")
:   Represents the status of how a window should be opened.

[WindowControlArea](enum.WindowControlArea.html "enum gpui::WindowControlArea")
:   A type of window control area that corresponds to the platform window.

[WindowDecorations](enum.WindowDecorations.html "enum gpui::WindowDecorations")
:   A type to describe the appearance of a window

[WindowKind](enum.WindowKind.html "enum gpui::WindowKind")
:   The kind of window to create

Constants[§](#constants)
------------------------

[KEYSTROKE\_PARSE\_EXPECTED\_MESSAGE](constant.KEYSTROKE_PARSE_EXPECTED_MESSAGE.html "constant gpui::KEYSTROKE_PARSE_EXPECTED_MESSAGE")
:   Sentence explaining what keystroke parser expects, starting with “Expected …”

[LOADING\_DELAY](constant.LOADING_DELAY.html "constant gpui::LOADING_DELAY")
:   The delay before showing the loading state.

[SHUTDOWN\_TIMEOUT](constant.SHUTDOWN_TIMEOUT.html "constant gpui::SHUTDOWN_TIMEOUT")
:   The duration for which futures returned from [Context::on\_app\_quit](struct.Context.html#method.on_app_quit "method gpui::Context::on_app_quit") can run before the application fully quits.

Traits[§](#traits)
------------------

[Action](trait.Action.html "trait gpui::Action")
:   Actions are used to implement keyboard-driven UI. When you declare an action, you can bind keys
    to the action in the keymap and listeners for that action in the element tree.

[Along](trait.Along.html "trait gpui::Along")
:   A trait for accessing the given unit along a certain axis.

[AnimationExt](trait.AnimationExt.html "trait gpui::AnimationExt")
:   An extension trait for adding the animation wrapper to both Elements and Components

[AppContext](trait.AppContext.html "trait gpui::AppContext")
:   The context trait, allows the different contexts in GPUI to be used
    interchangeably for certain operations.

[AsKeystroke](trait.AsKeystroke.html "trait gpui::AsKeystroke")
:   This is a helper trait so that we can simplify the implementation of some functions

[Asset](trait.Asset.html "trait gpui::Asset")
:   A trait for asynchronous asset loading.

[AssetSource](trait.AssetSource.html "trait gpui::AssetSource")
:   A source of assets for this app to use.

[BorrowAppContext](trait.BorrowAppContext.html "trait gpui::BorrowAppContext")
:   A helper trait for auto-implementing certain methods on contexts that
    can be used interchangeably.

[Element](trait.Element.html "trait gpui::Element")
:   Implemented by types that participate in laying out and painting the contents of a window.
    Elements form a tree and are laid out according to web-based layout rules, as implemented by Taffy.
    You can create custom elements by implementing this trait, see the module-level documentation
    for more details.

[EntityInputHandler](trait.EntityInputHandler.html "trait gpui::EntityInputHandler")
:   Implement this trait to allow views to handle textual input when implementing an editor, field, etc.

[EventEmitter](trait.EventEmitter.html "trait gpui::EventEmitter")
:   A trait for tying together the types of a GPUI entity and the events it can
    emit.

[Flatten](trait.Flatten.html "trait gpui::Flatten")
:   A flatten equivalent for anyhow `Result`s.

[Focusable](trait.Focusable.html "trait gpui::Focusable")
:   Focusable allows users of your view to easily
    focus it (using window.focus\_view(cx, view))

[FutureExt](trait.FutureExt.html "trait gpui::FutureExt")
:   Extensions for Future types that provide additional combinators and utilities.

[Global](trait.Global.html "trait gpui::Global")
:   A marker trait for types that can be stored in GPUI’s global state.

[Half](trait.Half.html "trait gpui::Half")
:   Provides a trait for types that can calculate half of their value.

[ImageCache](trait.ImageCache.html "trait gpui::ImageCache")
:   An object that can handle the caching and unloading of images.
    Implementations of this trait should ensure that images are removed from all windows when they are no longer needed.

[ImageCacheProvider](trait.ImageCacheProvider.html "trait gpui::ImageCacheProvider")
:   An object that can create an ImageCache during the render phase.
    See the ImageCache trait for more information.

[InputEvent](trait.InputEvent.html "trait gpui::InputEvent")
:   An event from a platform input source.

[InputHandler](trait.InputHandler.html "trait gpui::InputHandler")
:   Zed’s interface for handling text input from the platform’s IME system
    This is currently a 1:1 exposure of the NSTextInputClient API:

[InteractiveElement](trait.InteractiveElement.html "trait gpui::InteractiveElement")
:   A trait for elements that want to use the standard GPUI event handlers that don’t
    require any state.

[IntoElement](trait.IntoElement.html "trait gpui::IntoElement")
:   Implemented by any type that can be converted into an element.

[IsEmpty](trait.IsEmpty.html "trait gpui::IsEmpty")

[IsZero](trait.IsZero.html "trait gpui::IsZero")
:   A trait for checking if a value is zero.

[KeyEvent](trait.KeyEvent.html "trait gpui::KeyEvent")
:   A key event from the platform.

[ManagedView](trait.ManagedView.html "trait gpui::ManagedView")
:   ManagedView is a view (like a Modal, Popover, Menu, etc.)
    where the lifecycle of the view is handled by another view.

[MouseEvent](trait.MouseEvent.html "trait gpui::MouseEvent")
:   A mouse event from the platform.

[Negate](trait.Negate.html "trait gpui::Negate")
:   Provides a trait for types that can negate their values.

[ParentElement](trait.ParentElement.html "trait gpui::ParentElement")
:   This is a helper trait to provide a uniform interface for constructing elements that
    can accept any number of any kind of child elements

[PlatformDisplay](trait.PlatformDisplay.html "trait gpui::PlatformDisplay")
:   A handle to a platform’s display, e.g. a monitor or laptop screen.

[PlatformKeyboardLayout](trait.PlatformKeyboardLayout.html "trait gpui::PlatformKeyboardLayout")
:   A trait for platform-specific keyboard layouts

[PlatformKeyboardMapper](trait.PlatformKeyboardMapper.html "trait gpui::PlatformKeyboardMapper")
:   A trait for platform-specific keyboard mappings

[Prompt](trait.Prompt.html "trait gpui::Prompt")
:   A prompt that can be rendered in the window.

[ReadGlobal](trait.ReadGlobal.html "trait gpui::ReadGlobal")
:   A trait for reading a global value from the context.

[Refineable](trait.Refineable.html "trait gpui::Refineable")
:   A trait for types that can be refined with partial updates.

[Render](trait.Render.html "trait gpui::Render")
:   An object that can be drawn to the screen. This is the trait that distinguishes “views” from
    other entities. Views are `Entity`’s which `impl Render` and drawn to the screen.

[RenderOnce](trait.RenderOnce.html "trait gpui::RenderOnce")
:   You can derive [`IntoElement`](trait.IntoElement.html "trait gpui::IntoElement") on any type that implements this trait.
    It is used to construct reusable `components` out of plain data. Think of
    components as a recipe for a certain pattern of elements. RenderOnce allows
    you to invoke this pattern, without breaking the fluent builder pattern of
    the element APIs.

[ScreenCaptureSource](trait.ScreenCaptureSource.html "trait gpui::ScreenCaptureSource")
:   A source of on-screen video content that can be captured.

[ScreenCaptureStream](trait.ScreenCaptureStream.html "trait gpui::ScreenCaptureStream")
:   A video stream captured from a screen.

[StatefulInteractiveElement](trait.StatefulInteractiveElement.html "trait gpui::StatefulInteractiveElement")
:   A trait for elements that want to use the standard GPUI interactivity features
    that require state.

[Styled](trait.Styled.html "trait gpui::Styled")
:   A trait for elements that can be styled.
    Use this to opt-in to a utility CSS-like styling API.

[StyledImage](trait.StyledImage.html "trait gpui::StyledImage")
:   Style an image element.

[UniformListDecoration](trait.UniformListDecoration.html "trait gpui::UniformListDecoration")
:   A decoration for a [`UniformList`](struct.UniformList.html "struct gpui::UniformList"). This can be used for various things,
    such as rendering indent guides, or other visual effects.

[UpdateGlobal](trait.UpdateGlobal.html "trait gpui::UpdateGlobal")
:   A trait for updating a global value in the context.

[VisualContext](trait.VisualContext.html "trait gpui::VisualContext")
:   This trait is used for the different visual contexts in GPUI that
    require a window to be present.

Functions[§](#functions)
------------------------

[anchored](fn.anchored.html "fn gpui::anchored")
:   anchored gives you an element that will avoid overflowing the window bounds.
    Its children should have no margin to avoid measurement issues.

[auto](fn.auto.html "fn gpui::auto")
:   Returns a `Length` representing an automatic length.

[background\_executor](fn.background_executor.html "fn gpui::background_executor")
:   Returns a background executor for the current platform.

[black](fn.black.html "fn gpui::black")
:   Pure black in [`Hsla`](struct.Hsla.html "struct gpui::Hsla")

[blue](fn.blue.html "fn gpui::blue")
:   The color blue in [`Hsla`](struct.Hsla.html "struct gpui::Hsla")

[bounce](fn.bounce.html "fn gpui::bounce")
:   Apply the given easing function, first in the forward direction and then in the reverse direction

[bounds](fn.bounds.html "fn gpui::bounds")
:   Create a bounds with the given origin and size

[canvas](fn.canvas.html "fn gpui::canvas")
:   Construct a canvas element with the given paint callback.
    Useful for adding short term custom drawing to a view.

[combine\_highlights](fn.combine_highlights.html "fn gpui::combine_highlights")
:   Combine and merge the highlights and ranges in the two iterators.

[deferred](fn.deferred.html "fn gpui::deferred")
:   Builds a `Deferred` element, which delays the layout and paint of its child.

[div](fn.div.html "fn gpui::div")
:   Construct a new [`Div`](struct.Div.html "struct gpui::Div") element

[ease\_in\_out](fn.ease_in_out.html "fn gpui::ease_in_out")
:   The quadratic ease-in-out function, which starts and ends slowly but speeds up in the middle

[ease\_out\_quint](fn.ease_out_quint.html "fn gpui::ease_out_quint")
:   The Quint ease-out function, which starts quickly and decelerates to a stop

[fallback\_prompt\_renderer](fn.fallback_prompt_renderer.html "fn gpui::fallback_prompt_renderer")
:   Use this function in conjunction with [App::set\_prompt\_builder](struct.App.html#method.set_prompt_builder "method gpui::App::set_prompt_builder") to force
    GPUI to always use the fallback prompt renderer.

[fill](fn.fill.html "fn gpui::fill")
:   Creates a filled quad with the given bounds and background color.

[font](fn.font.html "fn gpui::font")
:   Get a [`Font`](struct.Font.html "struct gpui::Font") for a given name.

[generate\_list\_of\_all\_registered\_actions](fn.generate_list_of_all_registered_actions.html "fn gpui::generate_list_of_all_registered_actions")
:   Generate a list of all the registered actions.
    Useful for transforming the list of available actions into a
    format suited for static analysis such as in validating keymaps, or
    generating documentation.

[green](fn.green.html "fn gpui::green")
:   The color green in [`Hsla`](struct.Hsla.html "struct gpui::Hsla")

[guess\_compositor](fn.guess_compositor.html "fn gpui::guess_compositor")
:   Return which compositor we’re guessing we’ll use.
    Does not attempt to connect to the given compositor

[hash](fn.hash.html "fn gpui::hash")
:   Use a quick, non-cryptographically secure hash function to get an identifier from data

[hsla](fn.hsla.html "fn gpui::hsla")
:   Construct an [`Hsla`](struct.Hsla.html "struct gpui::Hsla") object from plain values

[image\_cache](fn.image_cache.html "fn gpui::image_cache")
:   An image cache element, all its child img elements will use the cache specified by this element.
    Note that this could as simple as passing an `Entity<T: ImageCache>`

[img](fn.img.html "fn gpui::img")
:   Create a new image element.

[is\_no\_action](fn.is_no_action.html "fn gpui::is_no_action")
:   Returns whether or not this action represents a removed key binding.

[linear](fn.linear.html "fn gpui::linear")
:   The linear easing function, or delta itself

[linear\_color\_stop](fn.linear_color_stop.html "fn gpui::linear_color_stop")
:   Creates a new linear color stop.

[linear\_gradient](fn.linear_gradient.html "fn gpui::linear_gradient")
:   Creates a LinearGradient background color.

[list](fn.list.html "fn gpui::list")
:   Construct a new list element

[opaque\_grey](fn.opaque_grey.html "fn gpui::opaque_grey")
:   Opaque grey in [`Hsla`](struct.Hsla.html "struct gpui::Hsla"), values will be clamped to the range [0, 1]

[outline](fn.outline.html "fn gpui::outline")
:   Creates a rectangle outline with the given bounds, border color, and a 1px border width

[pattern\_slash](fn.pattern_slash.html "fn gpui::pattern_slash")
:   Creates a hash pattern background

[percentage](fn.percentage.html "fn gpui::percentage")
:   Generate a `Radian` from a percentage of a full circle.

[phi](fn.phi.html "fn gpui::phi")
:   Returns the Golden Ratio, i.e. `~(1.0 + sqrt(5.0)) / 2.0`.

[point](fn.point.html "fn gpui::point")
:   Constructs a new `Point<T>` with the given x and y coordinates.

[pulsating\_between](fn.pulsating_between.html "fn gpui::pulsating_between")
:   A custom easing function for pulsating alpha that slows down as it approaches 0.1

[px](fn.px.html "fn gpui::px")
:   Constructs a `Pixels` value representing a length in pixels.

[quad](fn.quad.html "fn gpui::quad")
:   Creates a quad with the given parameters.

[quadratic](fn.quadratic.html "fn gpui::quadratic")
:   The quadratic easing function, delta \* delta

[radians](fn.radians.html "fn gpui::radians")
:   Create a `Radian` from a raw value

[red](fn.red.html "fn gpui::red")
:   The color red in [`Hsla`](struct.Hsla.html "struct gpui::Hsla")

[relative](fn.relative.html "fn gpui::relative")
:   Constructs a `DefiniteLength` representing a relative fraction of a parent size.

[rems](fn.rems.html "fn gpui::rems")
:   Constructs a `Rems` value representing a length in rems.

[retain\_all](fn.retain_all.html "fn gpui::retain_all")
:   Constructs a retain-all image cache that uses the element state associated with the given ID.

[rgb](fn.rgb.html "fn gpui::rgb")
:   Convert an RGB hex color code number to a color type

[rgba](fn.rgba.html "fn gpui::rgba")
:   Convert an RGBA hex color code number to [`Rgba`](struct.Rgba.html "struct gpui::Rgba")

[size](fn.size.html "fn gpui::size")
:   Constructs a new `Size<T>` with the provided width and height.

[solid\_background](fn.solid_background.html "fn gpui::solid_background")
:   Creates a solid background color.

[surface](fn.surface.html "fn gpui::surface")
:   Create a new surface element.

[svg](fn.svg.html "fn gpui::svg")
:   Create a new SVG element.

[transparent\_black](fn.transparent_black.html "fn gpui::transparent_black")
:   Transparent black in [`Hsla`](struct.Hsla.html "struct gpui::Hsla")

[transparent\_white](fn.transparent_white.html "fn gpui::transparent_white")
:   Transparent white in [`Hsla`](struct.Hsla.html "struct gpui::Hsla")

[uniform\_list](fn.uniform_list.html "fn gpui::uniform_list")
:   uniform\_list provides lazy rendering for a set of items that are of uniform height.
    When rendered into a container with overflow-y: hidden and a fixed (or max) height,
    uniform\_list will only render the visible subset of items.

[white](fn.white.html "fn gpui::white")
:   Pure white in [`Hsla`](struct.Hsla.html "struct gpui::Hsla")

[yellow](fn.yellow.html "fn gpui::yellow")
:   The color yellow in [`Hsla`](struct.Hsla.html "struct gpui::Hsla")

Type Aliases[§](#types)
-----------------------

[AlignSelf](type.AlignSelf.html "type gpui::AlignSelf")
:   Used to control how the specified nodes is aligned.
    Overrides the parent Node’s `AlignItems` property.
    For Flexbox it controls alignment in the cross axis
    For Grid it controls alignment in the block axis

[ImageLoadingTask](type.ImageLoadingTask.html "type gpui::ImageLoadingTask")
:   An image loading task associated with an image cache.

[ImgResourceLoader](type.ImgResourceLoader.html "type gpui::ImgResourceLoader")
:   A type alias to the resource loader that the `img()` element uses.

[InspectorRenderer](type.InspectorRenderer.html "type gpui::InspectorRenderer")
:   Function set on `App` to render the inspector UI.

[JustifyContent](type.JustifyContent.html "type gpui::JustifyContent")
:   Sets the distribution of space between and around content items
    For Flexbox it controls alignment in the main axis
    For Grid it controls alignment in the inline axis

[JustifyItems](type.JustifyItems.html "type gpui::JustifyItems")
:   Used to control how child nodes are aligned.
    Does not apply to Flexbox, and will be ignored if specified on a flex container
    For Grid it controls alignment in the inline axis

[JustifySelf](type.JustifySelf.html "type gpui::JustifySelf")
:   Used to control how the specified nodes is aligned.
    Overrides the parent Node’s `JustifyItems` property.
    Does not apply to Flexbox, and will be ignored if specified on a flex child
    For Grid it controls alignment in the inline axis

[Result](type.Result.html "type gpui::Result")
:   `Result<T, Error>`

[Transform](type.Transform.html "type gpui::Transform")
:   Alias for `euclid::default::Transform2D<f32>`

Attribute Macros[§](#attributes)
--------------------------------

[ctor](attr.ctor.html "attr gpui::ctor")

[test](attr.test.html "attr gpui::test")
:   `#[gpui::test]` can be used to annotate test functions that run with GPUI support.

Derive Macros[§](#derives)
--------------------------

[Action](derive.Action.html "derive gpui::Action")
:   `Action` derive macro - see the trait documentation for details.

[AppContext](derive.AppContext.html "derive gpui::AppContext")
:   #[derive(AppContext)] is used to create a context out of anything that holds a `&mut App`
    Note that a `#[app]` attribute is required to identify the variable holding the &mut App.

[IntoElement](derive.IntoElement.html "derive gpui::IntoElement")
:   #[derive(IntoElement)] is used to create a Component out of anything that implements
    the `RenderOnce` trait.

[Refineable](derive.Refineable.html "derive gpui::Refineable")

[VisualContext](derive.VisualContext.html "derive gpui::VisualContext")
:   #[derive(VisualContext)] is used to create a visual context out of anything that holds a `&mut Window` and
    implements `AppContext`
    Note that a `#[app]` and a `#[window]` attribute are required to identify the variables holding the &mut App,
    and &mut Window respectively.