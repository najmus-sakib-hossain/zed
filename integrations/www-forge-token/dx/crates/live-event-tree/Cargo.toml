[package]
name = "live-event-tree"
version.workspace = true
edition.workspace = true
description = "Hierarchical event segmentation for live streams"

[dependencies]
dx-core = { workspace = true }
async-trait = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
