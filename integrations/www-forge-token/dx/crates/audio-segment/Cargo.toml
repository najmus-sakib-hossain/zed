[package]
name = "audio-segment"
version.workspace = true
edition.workspace = true
description = "Silence removal from audio — 40% savings by trimming dead air"

[dependencies]
dx-core = { workspace = true }
async-trait = { workspace = true }
audio-compress = { workspace = true }

[dev-dependencies]
tokio = { workspace = true }
